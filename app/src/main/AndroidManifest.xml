<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppBanHangOnline"
        android:name=".App"
        tools:targetApi="31">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GiayActivity" />
        <activity android:name=".activity.NonActivity" />
        <activity android:name=".activity.AoActivity" />
        <activity android:name=".activity.QuanActivity" />
        <activity android:name=".activity.DepActivity" />
        <activity android:name=".activity.ThongTinActivity" />
        <activity android:name=".activity.LienHeActivity" />
        <activity android:name=".activity.ChiTietSanPhamActivity" />
        <activity android:name=".activity.GiohangActivity" />
        <activity android:name=".activity.Thongtinkhachhang" />
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.ProfileActivity" />
    </application>

</manifest>